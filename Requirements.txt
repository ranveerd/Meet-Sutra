# ============================================================
# AI Meeting Assistant - requirements.txt
# Python 3.10+ recommended
# NOTE: Whisper and yt-dlp also need FFmpeg installed on your system
#   Windows: winget install ffmpeg   |  macOS: brew install ffmpeg
#   Ubuntu:  sudo apt install ffmpeg
# ============================================================

# --- UI ---
streamlit>=1.38.0

# --- Audio input (YouTube URLs / uploaded files) ---
yt-dlp>=2024.8.6
pydub>=0.25.1

# --- Local transcription (Whisper) ---
openai-whisper>=20240930
torch>=2.2.0
numpy>=1.26.0

# --- Language detection (to decide if Hindi -> English is needed) ---
langdetect>=1.0.9

# --- LangChain (LCEL chains) + Mistral API ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langchain-mistralai>=0.2.0

# --- RAG: vector store + HuggingFace embeddings ---
chromadb>=0.5.0
langchain-chroma>=0.1.4
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0

# --- Export (PDF / TXT) ---
fpdf2>=2.8.0

# --- Config / environment variables (e.g. MISTRAL_API_KEY) ---
python-dotenv>=1.0.1
